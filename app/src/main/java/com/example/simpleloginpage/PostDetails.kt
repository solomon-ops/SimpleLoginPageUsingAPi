package com.example.simpleloginpage

data class PostDetails(val title : String,
                       val userId : Int,
                       val body : String,
                       val postId : Int,
                      )
